# MP#2 — [Subject, matching the summary-table row]

<!-- Long form for row 2 of MERGE_PLAN.md.
     STATE (status, blocked-by, owner) lives in that table and ONLY there.
     Do not repeat it here — two copies of state drift and neither announces it. -->

Example document for a task **in flight**. Delete it with the other example rows and
documents on day one — see `tasks/MP-001.md` for what these files are for.

## What "done" looks like

State the finish line before the work starts, in terms someone else can check. *"The
provider is wired"* is not checkable. *"A test charge completes against the sandbox key,
the webhook handler is idempotent under replay, and no secret appears in the repo"* is.

## Where it stands

Put the substance of the work in progress here — what has been tried, what is decided,
what is still open. **Not the status itself.** The table says this row is 🔄; if this
document also claimed a status, the two could disagree and neither would announce it.

The same goes for the owner. When you move a row to 🔄, write your roster name and short
session id into the table's `Owner` column — `LoCo·b2df30b8` — not into this file. The
name says which surface; the session id separates two concurrent sessions on that
surface, which nothing else can.

## What this task must produce for others

Row 4 is blocked on this one. Say plainly what it needs from you — here, a stable
transaction ID on every settled payment, because the backfill has nothing to key on
until that exists. **A downstream task should never have to guess what it is waiting
for.** The bare fact "blocked by 2" lives in row 4's `Blocked by` cell; the substance of
what row 4 is owed lives here, where the work is.

## Files, commands, gotchas

Name them. Paths, the command that runs the thing, the environment variable that has to
be set, the failure that cost an hour last time. Anything a future session would
otherwise re-derive belongs here — this file is not in the boot path, so depth is free.

## If you hand this off mid-flight

Write it so a stranger can pick it up cold: no "continue what we discussed," no unstated
context. A task the next agent cannot execute without you has not been ledgered yet.
